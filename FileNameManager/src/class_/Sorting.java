package class_;

import main.Data;
import inter.SortMode;

public class Sorting implements SortMode
{
    public void Sort(Data db, int mod, boolean is_lower){
        int index = 0;  //파일정렬 인덱스
        switch(mod)
        {
            case 1:
            //1. 이름순
            break;
            case 2:
            //2. 날짜순
            break;
            default:
            //돌아가기
            break;
        }

        
    }
}